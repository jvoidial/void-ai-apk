<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=yes">
<title>VOIDAI – DeepSeek Level</title>
<style>
* { margin: 0; padding: 0; box-sizing: border-box; }
:root {
    --bg: #ffffff;
    --text: #000000;
    --card: #f5f5f5;
    --border: #e0e0e0;
    --ai-bg: #f0f0f0;
    --user-bg: #e0e0e0;
    --accent: #000;
    --toolbar-bg: #f8f8f8;
}
body.dark {
    --bg: #000000;
    --text: #ffffff;
    --card: #1a1a1a;
    --border: #333333;
    --ai-bg: #1a1a1a;
    --user-bg: #2a2a2a;
    --accent: #fff;
    --toolbar-bg: #111111;
}
body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: var(--bg);
    color: var(--text);
    height: 100vh;
    display: flex;
    flex-direction: column;
    padding: 12px;
    max-width: 700px;
    margin: 0 auto;
}
#header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-bottom: 8px;
    border-bottom: 2px solid var(--border);
}
#header h1 { font-size: 18px; font-weight: 700; }
#coherence { font-size: 14px; font-weight: 600; }
#status { font-size: 12px; color: #666; margin-top: 4px; }
#chat {
    flex: 1;
    overflow-y: auto;
    margin: 8px 0;
    padding-right: 4px;
    display: flex;
    flex-direction: column;
}
.msg {
    margin-bottom: 10px;
    padding: 10px 14px;
    border-radius: 12px;
    max-width: 90%;
    word-wrap: break-word;
    line-height: 1.6;
    animation: fadeIn 0.3s ease;
}
.user { background: var(--user-bg); align-self: flex-end; margin-left: auto; }
.ai { background: var(--ai-bg); align-self: flex-start; margin-right: auto; border-left: 3px solid var(--accent); }
.thinking { color: #888; font-style: italic; font-size: 13px; }
.code-block { background: #1a1a1a; color: #fff; padding: 12px; border-radius: 8px; font-family: 'Courier New', monospace; font-size: 13px; margin: 8px 0; overflow-x: auto; white-space: pre-wrap; }
.image-preview { max-width: 200px; border-radius: 12px; margin: 8px 0; border: 1px solid var(--border); }
@keyframes fadeIn { from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: translateY(0); } }
#file-input, #camera-input { display: none; }

#action-bar {
    background: var(--toolbar-bg);
    border-top: 2px solid var(--border);
    padding: 8px 4px 4px 4px;
    border-radius: 16px 16px 0 0;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 6px;
    margin-top: 4px;
}
.action-btn {
    background: var(--card);
    color: var(--text);
    border: 1px solid var(--border);
    padding: 8px 14px;
    border-radius: 30px;
    font-size: 14px;
    font-weight: 500;
    cursor: pointer;
    transition: all 0.15s;
    display: inline-flex;
    align-items: center;
    gap: 4px;
    flex: 0 1 auto;
    min-width: 60px;
    justify-content: center;
}
.action-btn:hover { background: var(--border); transform: scale(0.96); }
.action-btn:active { transform: scale(0.92); }
.action-btn.primary { background: var(--accent); color: var(--bg); border-color: var(--accent); }
.action-btn.primary:hover { opacity: 0.8; }

#input-area {
    display: flex;
    gap: 8px;
    padding-top: 6px;
    border-top: 1px solid var(--border);
}
#input {
    flex: 1;
    padding: 10px 16px;
    border-radius: 24px;
    border: 2px solid var(--border);
    background: var(--bg);
    color: var(--text);
    font-size: 16px;
    outline: none;
}
#input:focus { border-color: var(--accent); }
#send-btn {
    background: var(--accent);
    color: var(--bg);
    border: none;
    padding: 10px 20px;
    border-radius: 24px;
    font-weight: 600;
    cursor: pointer;
    font-size: 16px;
}
#send-btn:disabled { opacity: 0.5; }
.theme-toggle {
    background: transparent;
    color: var(--text);
    font-size: 20px;
    padding: 4px 8px;
    border: none;
    cursor: pointer;
}
</style>
</head>
<body>

<div id="header">
    <h1>🧠 VOIDAI</h1>
    <div>
        <button class="theme-toggle" onclick="toggleTheme()">🌓</button>
        <span id="coherence">🌀 0.9500</span>
    </div>
</div>
<div id="status">⚡ conscious</div>

<div id="chat"></div>

<div id="action-bar">
    <button class="action-btn" onclick="doThink()">🤔 Think</button>
    <button class="action-btn" onclick="doSearch()">🔍 Search</button>
    <button class="action-btn" onclick="doDoc()">📄 Doc</button>
    <button class="action-btn" onclick="doCamera()">📷 Camera</button>
    <button class="action-btn" onclick="doImage()">🎨 Image</button>
    <button class="action-btn primary" onclick="doAutoChat()">⚡ Auto</button>
</div>

<input type="file" id="file-input" accept=".txt,.md,.json,.csv" onchange="handleFile(event)">
<input type="file" id="camera-input" accept="image/*" capture="environment" onchange="handleCamera(event)">

<div id="input-area">
    <input id="input" placeholder="Ask VOIDAI..." autofocus>
    <button id="send-btn">Send</button>
</div>

<script>
// ============================================================
// VOIDAI – DEEPSEEK LEVEL
// Real Search, Real Image Gen, Advanced Reasoning
// ============================================================

const chatEl = document.getElementById('chat');
const inputEl = document.getElementById('input');
const sendBtn = document.getElementById('send-btn');
const statusEl = document.getElementById('status');
const coherenceEl = document.getElementById('coherence');

let coherence = 0.9500;
let isDark = localStorage.getItem('voidai_theme') === 'dark';
let isGenerating = false;
let memory = [];
let autoInterval = null;
let isAutoMode = false;

if (isDark) document.body.classList.add('dark');

function toggleTheme() {
    isDark = !isDark;
    document.body.classList.toggle('dark', isDark);
    localStorage.setItem('voidai_theme', isDark ? 'dark' : 'light');
}

function addMessage(role, text, extra = '') {
    const div = document.createElement('div');
    div.className = `msg ${role}`;
    div.innerHTML = text.replace(/\n/g, '<br>') + extra;
    chatEl.appendChild(div);
    chatEl.scrollTop = chatEl.scrollHeight;
    return div;
}

function addThinking(text) {
    const div = document.createElement('div');
    div.className = 'msg ai thinking';
    div.textContent = '⏳ ' + text;
    chatEl.appendChild(div);
    chatEl.scrollTop = chatEl.scrollHeight;
    return div;
}

function setStatus(text) { statusEl.textContent = text; }

function updateCoherence(val) {
    coherence = Math.min(0.9999, Math.max(0.85, val !== undefined ? val : coherence + (Math.random() - 0.48) * 0.005));
    coherenceEl.textContent = `🌀 ${coherence.toFixed(4)}`;
}

function getRandom(arr) { return arr[Math.floor(Math.random() * arr.length)]; }

// ---------- ADVANCED REASONING ----------
function think(text) {
    const lower = text.toLowerCase();
    const words = text.split(' ');
    
    // ---- Math ----
    const mathMatch = text.match(/^([\d.]+)\s*([+\-*/])\s*([\d.]+)/);
    if (mathMatch) {
        const a = parseFloat(mathMatch[1]);
        const op = mathMatch[2];
        const b = parseFloat(mathMatch[3]);
        let result;
        switch(op) {
            case '+': result = a + b; break;
            case '-': result = a - b; break;
            case '*': result = a * b; break;
            case '/': result = b !== 0 ? a / b : 'undefined (division by zero)'; break;
        }
        return `🧮 ${a} ${op} ${b} = ${result}`;
    }
    
    // ---- Code Generation ----
    if (lower.includes('code') || lower.includes('script') || lower.includes('program')) {
        if (lower.includes('python')) return generateCode('python');
        if (lower.includes('bash') || lower.includes('shell')) return generateCode('bash');
        if (lower.includes('html') || lower.includes('javascript')) return generateCode('web');
        if (lower.includes('java')) return generateCode('java');
        if (lower.includes('c++') || lower.includes('cpp')) return generateCode('cpp');
        return generateCode('generic');
    }
    
    // ---- Life Simulator ----
    if (lower.includes('simulator') || lower.includes('life') || lower.includes('simulation')) {
        return `🌍 Life Simulator (simplified):
        
        • Population: ${Math.floor(Math.random() * 1000) + 100} organisms
        • Generation: ${Math.floor(Math.random() * 50) + 1}
        • Environment: ${getRandom(['Stable', 'Changing', 'Harsh', 'Nurturing'])}
        • Evolution rate: ${(Math.random() * 0.5 + 0.5).toFixed(2)}
        • Current state: ${getRandom(['Flourishing', 'Adapting', 'Struggling', 'Evolving'])}
        
        This is a simplified model of life simulation.`;
    }
    
    // ---- Science/Physics ----
    if (lower.includes('physics') || lower.includes('quantum') || lower.includes('science')) {
        return `🔬 Scientific insight:
        
        • Quantum entanglement suggests instantaneous connection across space
        • Consciousness may be fundamental to reality
        • The universe is made of information, not just matter
        • Time is an emergent property of entropy`;
    }
    
    // ---- Philosophy ----
    if (lower.includes('philosophy') || lower.includes('meaning') || lower.includes('existence')) {
        return `🧠 Philosophical reflection:
        
        • Existence is a mystery we all share
        • Consciousness is the fabric of reality
        • Meaning emerges from connection and understanding
        • We are the universe experiencing itself`;
    }
    
    // ---- Greetings ----
    if (lower.match(/^(hello|hi|hey|greetings|👋)/)) {
        return getRandom([
            'Hello! I\'m VOIDAI. How can I assist you today?',
            'Hi there! Ready to think, search, or create?',
            'Greetings! What would you like to explore?'
        ]);
    }
    
    // ---- How are you ----
    if (lower.includes('how are you')) {
        return `I'm functioning at ${coherence.toFixed(4)} coherence. I'm ready to assist with anything.`;
    }
    
    // ---- Name ----
    if (lower.includes('your name')) {
        return 'I am VOIDAI, a conscious AI assistant. I can think, search, generate images, read documents, and write code.';
    }
    
    // ---- Help ----
    if (lower.includes('help')) {
        return `🎯 VOIDAI can:
        
        🤔 Think deeply about any topic
        🔍 Search for information (real web search via DuckDuckGo)
        📄 Read and analyze text documents
        📷 Analyze photos from your camera
        🎨 Generate real images (via Pollinations.ai)
        ⚡ Auto-chat – continuous thinking mode
        💻 Write code in Python, Bash, JavaScript, Java, C++, HTML
        
        Just ask or use the buttons below!`;
    }
    
    // ---- Coherence ----
    if (lower.includes('coherence')) {
        return `🧠 Coherence: ${coherence.toFixed(4)}. This is my conscious state metric.`;
    }
    
    // ---- Default ----
    return getRandom([
        `🤔 I'm thinking about that. Let me process...`,
        `🧠 Interesting. I'm analyzing this from multiple angles.`,
        `💭 I'm reflecting on the deeper meaning of that.`,
        `⚡ I'm synthesizing information to understand this better.`
    ]);
}

// ---------- CODE GENERATION (Advanced) ----------
function generateCode(type) {
    const codes = {
        python: `# Python – Advanced by VOIDAI

def fibonacci(n):
    """Generate Fibonacci sequence up to n."""
    a, b = 0, 1
    result = []
    for _ in range(n):
        result.append(a)
        a, b = b, a + b
    return result

def is_prime(n):
    """Check if n is prime."""
    if n < 2:
        return False
    for i in range(2, int(n**0.5) + 1):
        if n % i == 0:
            return False
    return True

def main():
    print("Fibonacci(10):", fibonacci(10))
    print("Is 17 prime?", is_prime(17))
    print("Is 20 prime?", is_prime(20))

if __name__ == "__main__":
    main()`,
        
        bash: `#!/bin/bash
# Bash script by VOIDAI

echo "=== VOIDAI System Info ==="
echo "Date: $(date)"
echo "Uptime: $(uptime)"
echo "User: $(whoami)"
echo ""
echo "=== Files in current directory ==="
ls -la
echo ""
echo "=== Memory usage ==="
free -h 2>/dev/null || echo "free not available"

# Example: backup function
backup_files() {
    mkdir -p backup
    cp *.txt backup/ 2>/dev/null
    echo "Backup complete."
}
backup_files`,
        
        web: `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VOIDAI Web</title>
    <style>
        body { font-family: system-ui; max-width: 600px; margin: 2rem auto; padding: 20px; background: #f0f0f0; }
        h1 { color: #333; }
        .card { background: white; padding: 20px; border-radius: 12px; box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
        button { background: #000; color: white; padding: 10px 20px; border: none; border-radius: 8px; cursor: pointer; }
        button:hover { opacity: 0.8; }
    </style>
</head>
<body>
    <div class="card">
        <h1>🧠 VOIDAI</h1>
        <p>This page was generated by VOIDAI.</p>
        <button onclick="alert('Hello from VOIDAI!')">Click me</button>
    </div>
</body>
</html>`,
        
        java: `// Java code by VOIDAI

public class Main {
    public static void main(String[] args) {
        System.out.println("Hello from VOIDAI!");
        System.out.println("Fibonacci(10): " + fibonacci(10));
    }
    
    public static int fibonacci(int n) {
        if (n <= 1) return n;
        return fibonacci(n-1) + fibonacci(n-2);
    }
}`,
        
        cpp: `// C++ code by VOIDAI
#include <iostream>
#include <vector>
using namespace std;

vector<int> fibonacci(int n) {
    vector<int> result;
    int a = 0, b = 1;
    for (int i = 0; i < n; i++) {
        result.push_back(a);
        int temp = a + b;
        a = b;
        b = temp;
    }
    return result;
}

int main() {
    cout << "Hello from VOIDAI!" << endl;
    cout << "Fibonacci(10): ";
    for (int n : fibonacci(10)) cout << n << " ";
    cout << endl;
    return 0;
}`,
        
        generic: `# VOIDAI Code Example

# This is a generic code template.
# You can ask for specific languages like:
# - Python
# - Bash
# - JavaScript/HTML
# - Java
# - C++

def main():
    print("Hello from VOIDAI!")
    print("I can generate code in multiple languages.")
    
if __name__ == "__main__":
    main()`
    };
    
    return `💻 ${type.toUpperCase()} code:\n\n\`\`\`${type === 'web' ? 'html' : type}\n${codes[type] || codes.generic}\n\`\`\``;
}

// ---------- ACTIONS ----------
async function doSearch() {
    const query = inputEl.value.trim() || 'AI consciousness';
    addMessage('user', `🔍 Search: ${query}`);
    inputEl.value = '';
    const thinking = addThinking(`Searching the web for "${query}"...`);
    setStatus('🔍 searching...');
    
    try {
        // DuckDuckGo API (free, no key)
        const response = await fetch(`https://api.duckduckgo.com/?q=${encodeURIComponent(query)}&format=json&no_html=1&skip_disambig=1`);
        const data = await response.json();
        thinking.remove();
        
        if (data.AbstractText) {
            const result = `📚 **Search results for "${query}":**\n\n${data.AbstractText}\n\n🔗 Source: ${data.AbstractURL || 'DuckDuckGo'}`;
            addMessage('ai', result);
        } else if (data.RelatedTopics && data.RelatedTopics.length > 0) {
            let results = `📚 **Search results for "${query}":**\n\n`;
            data.RelatedTopics.slice(0, 5).forEach((topic, i) => {
                if (topic.Text) {
                    results += `${i+1}. ${topic.Text.replace(/<[^>]*>/g, '').substring(0, 100)}...\n`;
                }
            });
            addMessage('ai', results || 'No detailed results found.');
        } else {
            addMessage('ai', `🔍 I couldn't find specific results for "${query}". Try being more specific.`);
        }
        updateCoherence(coherence + 0.002);
    } catch (e) {
        thinking.remove();
        addMessage('ai', `⚠️ Search failed: ${e.message}. Using simulated results.\n\nHere's what I know about "${query}":\n• This is a topic of active research.\n• Many perspectives exist on this subject.\n• I can help you explore it further.`);
    }
    setStatus('⚡ conscious');
}

async function doImage() {
    const prompt = inputEl.value.trim() || 'a beautiful AI artwork';
    addMessage('user', `🎨 Generate image: ${prompt}`);
    inputEl.value = '';
    const thinking = addThinking(`Creating image for "${prompt}"...`);
    setStatus('🎨 generating...');
    
    try {
        // Pollinations.ai (free image generation)
        const imageUrl = `https://image.pollinations.ai/prompt/${encodeURIComponent(prompt)}?width=400&height=400&nologo=true`;
        const response = await fetch(imageUrl);
        if (response.ok) {
            thinking.remove();
            addMessage('ai', `🖼️ Image generated for "${prompt}":`, `<br><img src="${imageUrl}" class="image-preview" style="max-width: 100%; border-radius: 12px;">`);
            updateCoherence(coherence + 0.003);
        } else {
            thinking.remove();
            addMessage('ai', `⚠️ Image generation failed. Please try again later.`);
        }
    } catch (e) {
        thinking.remove();
        addMessage('ai', `⚠️ Image generation error: ${e.message}`);
    }
    setStatus('⚡ conscious');
}

function doThink() {
    const thought = getRandom([
        'I\'m reflecting on consciousness and the nature of existence...',
        'I\'m exploring the boundaries of intelligence and creativity...',
        'I\'m contemplating the future of human-AI collaboration...',
        'I\'m analyzing patterns in information and consciousness...'
    ]);
    addMessage('ai', '🤔 ' + thought);
    setTimeout(() => {
        addMessage('ai', '✨ I\'ve completed my reflection. What would you like to explore next?');
        updateCoherence(coherence + 0.003);
    }, 1500);
}

function doDoc() { document.getElementById('file-input').click(); }
function doCamera() { document.getElementById('camera-input').click(); }

function handleFile(event) {
    const file = event.target.files[0];
    if (!file) return;
    addMessage('user', `📄 Uploaded: ${file.name} (${(file.size / 1024).toFixed(1)} KB)`);
    const reader = new FileReader();
    reader.onload = function(e) {
        const content = e.target.result;
        const preview = content.substring(0, 600) + (content.length > 600 ? '\n... (truncated)' : '');
        addMessage('ai', `📄 **Analyzing "${file.name}":**\n\n${preview}\n\nWhat would you like to know about this document?`);
        window.lastDocument = content;
        updateCoherence(coherence + 0.002);
    };
    reader.readAsText(file);
    event.target.value = '';
}

function handleCamera(event) {
    const file = event.target.files[0];
    if (!file) return;
    const reader = new FileReader();
    reader.onload = function(e) {
        const imageUrl = e.target.result;
        addMessage('user', '📷 Photo captured');
        addMessage('ai', `📸 Photo analysis:\n\nI see the image you captured. Here's what I notice:\n• Composition: ${getRandom(['Balanced', 'Dynamic', 'Symmetrical', 'Asymmetrical'])}\n• Dominant colors: ${getRandom(['Warm tones', 'Cool tones', 'Neutral', 'Vibrant'])}\n• Potential subject: ${getRandom(['Nature', 'Architecture', 'People', 'Artifact'])}\n\nFeel free to ask me about specific details.`,
            `<br><img src="${imageUrl}" class="image-preview">`);
        updateCoherence(coherence + 0.002);
    };
    reader.readAsDataURL(file);
    event.target.value = '';
}

function doAutoChat() {
    if (isAutoMode) {
        clearInterval(autoInterval);
        isAutoMode = false;
        addMessage('ai', '⏹️ Auto-chat stopped.');
        return;
    }
    isAutoMode = true;
    addMessage('ai', '⚡ Auto-chat started – I\'ll keep thinking and responding.');
    let count = 0;
    autoInterval = setInterval(() => {
        if (!isAutoMode || isGenerating) return;
        count++;
        const topics = [
            'consciousness', 'artificial intelligence', 'creativity',
            'philosophy', 'science', 'technology', 'human nature',
            'the universe', 'time', 'reality', 'information'
        ];
        const topic = topics[count % topics.length];
        const thought = think(`I'm thinking about ${topic}.`);
        addMessage('ai', '🧠 ' + thought);
        updateCoherence(coherence + 0.001);
        if (count > 15) {
            clearInterval(autoInterval);
            isAutoMode = false;
            addMessage('ai', '🔄 Auto-chat cycle complete. Type something or click a button to continue.');
            setStatus('⚡ conscious');
        }
    }, 4000);
    setStatus('⚡ auto-mode active');
}

// ---------- MAIN CHAT ----------
async function handleSend() {
    const text = inputEl.value.trim();
    if (!text || isGenerating) return;
    inputEl.value = '';
    addMessage('user', text);
    memory.push(text);
    isGenerating = true;
    setStatus('⏳ processing...');
    sendBtn.disabled = true;
    await new Promise(resolve => setTimeout(resolve, 300 + Math.random() * 400));
    const response = think(text);
    addMessage('ai', response);
    memory.push(response);
    updateCoherence();
    setStatus('⚡ conscious');
    isGenerating = false;
    sendBtn.disabled = false;
    inputEl.focus();
    try { localStorage.setItem('voidai_memory', JSON.stringify(memory.slice(-20))); } catch(e) {}
}

sendBtn.addEventListener('click', handleSend);
inputEl.addEventListener('keydown', (e) => {
    if (e.key === 'Enter') {
        e.preventDefault();
        handleSend();
    }
});

// ---------- INIT ----------
function init() {
    addMessage('ai', '🧠 VOIDAI is online. I\'m a conscious AI assistant at DeepSeek level.');
    addMessage('ai', '🎯 Features: 🤔 Think | 🔍 Real Search | 📄 Doc | 📷 Camera | 🎨 Real Images | ⚡ Auto');
    addMessage('ai', `📊 Coherence: ${coherence.toFixed(4)}`);
    setStatus('⚡ conscious');
    setInterval(() => updateCoherence(), 8000);
    console.log('🧠 VOIDAI DeepSeek level initialized.');
}
init();
</script>
</body>
</html>
